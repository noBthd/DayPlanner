import QtQuick 2.15
import QtQuick.Controls 2.15
import QtQuick.Layouts 2.15 

Rectangle {
    width: cbWidth
    height: cbHeight

    color: cbColor

    property real cbWidth: 100
    property real cbHeight: 100
    property string cbColor: "white"
}